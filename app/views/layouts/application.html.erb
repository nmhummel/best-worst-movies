<!DOCTYPE html>
<html>
  <head>
    <title>Schlock Flop</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'stranger', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
<header class="masthead clear">
    <%= render 'layouts/header' %>
    </header>
    <br><br>

    
		    <% flash.each do |name, msg| %>
			<%= content_tag(:div, msg, class: "alert alert-info") %>
		  <% end %>

<main class="main-area">
  <div class="centered">
    <section class="cards">
      <article class="card">

		    <%= yield %>

    </section>
  </div>
</main>  
     
    <br><br>
    <%= render 'layouts/footer' %>
	


  </body>
</html>

